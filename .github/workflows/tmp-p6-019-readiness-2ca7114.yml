name: RETIRED TEMP OPS-P6-019 exact-main production readiness diagnostic

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Retired after read-only diagnostic run 31712486367.'
          echo 'Authoritative receipt: staging-review/config/production-authorization/readiness-diagnostic.json'
          exit 1
