name: P5-07A status document

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

permissions:
  contents: write

jobs:
  update:
    if: github.head_ref == 'p5-07a-status-handoff' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: p5-07a-status-handoff
          fetch-depth: 0
      - name: Mark P5-07A complete
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('docs/P5_07A_CANONICAL_APPLICATION_RETENTION_INVENTORY.md')
          source = path.read_text()
          old = '**Status:** Active — repository inventory  '
          new = '**Status:** Completed in PR #243  '
          if old not in source:
              raise SystemExit('P5-07A status marker is missing')
          path.write_text(source.replace(old, new, 1))
          PY
      - name: Commit status document
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/P5_07A_CANONICAL_APPLICATION_RETENTION_INVENTORY.md
          git commit -m "P5-07A: mark inventory complete"
          git push origin HEAD:p5-07a-status-handoff
